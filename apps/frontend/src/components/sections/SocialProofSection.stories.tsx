import type { Meta, StoryObj } from "@storybook/react/dist/index";
import { SocialProofSection } from "./SocialProofSection";

/**
 * 🍍 JOLANANAS - SocialProofSection
 * ==============================
 * Auto-generated Storybook file for SocialProofSection.
 */
const meta: Meta<typeof SocialProofSection> = {
  title: "JOLANANAS/Sections/SocialProofSection",
  component: SocialProofSection,
  parameters: {
    layout: "centered",
  },
  tags: ["autodocs"],
};

export default meta;
type Story = StoryObj<typeof SocialProofSection>;

export const Default: Story = {
  args: {},
};
