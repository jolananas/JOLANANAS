import type { Meta, StoryObj } from "@storybook/react/dist/index";
import { SiteGatekeeper } from "./SiteGatekeeper";

/**
 * 🍍 JOLANANAS - SiteGatekeeper
 * ==============================
 * Auto-generated Storybook file for SiteGatekeeper.
 */
const meta: Meta<typeof SiteGatekeeper> = {
  title: "JOLANANAS/Layout/SiteGatekeeper",
  component: SiteGatekeeper,
  parameters: {
    layout: "centered",
  },
  tags: ["autodocs"],
};

export default meta;
type Story = StoryObj<typeof SiteGatekeeper>;

export const Default: Story = {
  args: {},
};
